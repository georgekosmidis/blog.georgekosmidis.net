// This file provides a global type alias so the unqualified `AppSettings` refers to the Models version
// It resolves ambiguity with the auto-generated `Blog.Builder.AppSettings` from the settings designer.

global using AppSettings = Blog.Builder.Models.AppSettings;
